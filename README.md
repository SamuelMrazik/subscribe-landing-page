# Subscribe Landing Page

A minimal static landing page with a subscribe button.

## Local preview

Open `index.html` in your browser, or run a local server:

```bash
npx serve .
```

## Deploy

This project is connected to Vercel for automatic deployments from GitHub.
